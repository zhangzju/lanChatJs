# How to use

You can use it in with your intranet fellows. First ,install the Nodejs:
```
sudo apt install nodejs
```

then, start the server locally:

```
node server.js
```

ok, your fellows can chat with you by opening the browser and typing the IP address of your server!
